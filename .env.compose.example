# Local development defaults only. Override every value outside a disposable local environment.
MYSQL_DATABASE=financial_platform
MYSQL_USER=financial_app
MYSQL_PASSWORD=replace-local-app-password
MYSQL_ROOT_PASSWORD=replace-local-root-password
BUSINESS_AGENT_SECRET_KEY=replace-with-a-long-random-local-secret
